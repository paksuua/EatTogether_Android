<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UI.Activity.MakeUrlActivity">

    <ImageView
        android:id="@+id/btn_make_url_back"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/ic_baseline_clear_24"
        android:layout_margin="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="20dp"
        android:text="방 만들기"
        android:layout_marginBottom="70dp"
        app:layout_constraintBottom_toTopOf="@id/btn_make_url"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tv_members"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="참여인원"
        android:layout_marginBottom="30dp"
        android:layout_marginStart="80dp"
        app:layout_constraintStart_toStartOf="@+id/btn_make_url"
        app:layout_constraintBottom_toTopOf="@+id/btn_make_url"/>

    <EditText
        android:id="@+id/edt_join_num"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:hint="4"
        android:textAlignment="center"
        app:layout_constraintTop_toTopOf="@+id/tv_members"
        app:layout_constraintBottom_toBottomOf="@+id/tv_members"
        app:layout_constraintStart_toEndOf="@+id/tv_members"
        app:layout_constraintEnd_toEndOf="@+id/btn_make_url" />

    <Button
        android:id="@+id/btn_make_url"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="10dp"
        android:text="입장코드 생성"
        android:clickable="false"
        android:layout_marginHorizontal="30dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/bg_gray"/>

</androidx.constraintlayout.widget.ConstraintLayout>